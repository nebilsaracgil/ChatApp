import React from "react";
import Chat from "../Components/Chat";


const Chat_1 = ({navigation}) => {
    const name = 'BasedSigma'
    
    return (
        <Chat name={name} />
    )
};

export default Chat_1;